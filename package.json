{
  "name": "shuiniuniu-workbench-app",
  "version": "1.0.0",
  "private": true,
  "description": "水妞妞的工作台安卓 App 打包项目",
  "scripts": {
    "prepare:web": "node tools/prepare-web.js",
    "cap:add:android": "npm run prepare:web && npx cap add android",
    "cap:sync": "npm run prepare:web && npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1"
  },
  "devDependencies": {}
}
